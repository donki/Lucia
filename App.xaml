<Application x:Class="SocLucia.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnMainWindowClose"
             Startup="OnStartup" Exit="OnExit">
    <Application.Resources>
        <ResourceDictionary>

            <!--
                Sistema visual sOCratic (constitucion 24 y E.5): la misma paleta indigo y los mismos
                nombres que Task Manager para Windows. ThemeManager sustituye las superficies y los
                textos en tema oscuro; ninguna ventana sabe en que tema esta.
            -->

            <!-- ============================ Paleta ============================ -->
            <SolidColorBrush x:Key="Primary" Color="#3525CD" />
            <SolidColorBrush x:Key="PrimaryLight" Color="#635BF2" />
            <SolidColorBrush x:Key="Accent" Color="#4F46E5" />
            <SolidColorBrush x:Key="OnPrimary" Color="#FFFFFF" />
            <SolidColorBrush x:Key="PageBackground" Color="#F8F9FA" />
            <SolidColorBrush x:Key="CardBackground" Color="#FFFFFF" />
            <SolidColorBrush x:Key="Separator" Color="#C7C4D8" />
            <SolidColorBrush x:Key="TextPrimary" Color="#191C1D" />
            <SolidColorBrush x:Key="TextSecondary" Color="#464555" />
            <SolidColorBrush x:Key="Success" Color="#27AE60" />
            <SolidColorBrush x:Key="Danger" Color="#BA1A1A" />
            <SolidColorBrush x:Key="MirrorBackground" Color="#1B1B22" />
            <SolidColorBrush x:Key="WarningSurface" Color="#FFF4E5" />
            <SolidColorBrush x:Key="Star" Color="#E5A100" />

            <!-- ============================ Banderas ============================ -->
            <!-- Windows no dibuja los emoji de bandera (salen «ES» y «US»): se dibujan a mano, 3:2. -->
            <DrawingImage x:Key="FlagEs">
                <DrawingImage.Drawing>
                    <DrawingGroup>
                        <GeometryDrawing Brush="#AA151B"><GeometryDrawing.Geometry><RectangleGeometry Rect="0,0,30,20" /></GeometryDrawing.Geometry></GeometryDrawing>
                        <GeometryDrawing Brush="#F1BF00"><GeometryDrawing.Geometry><RectangleGeometry Rect="0,5,30,10" /></GeometryDrawing.Geometry></GeometryDrawing>
                    </DrawingGroup>
                </DrawingImage.Drawing>
            </DrawingImage>
            <DrawingImage x:Key="FlagUs">
                <DrawingImage.Drawing>
                    <DrawingGroup>
                        <GeometryDrawing Brush="#FFFFFF"><GeometryDrawing.Geometry><RectangleGeometry Rect="0,0,30,20" /></GeometryDrawing.Geometry></GeometryDrawing>
                        <GeometryDrawing Brush="#B22234">
                            <GeometryDrawing.Geometry>
                                <GeometryGroup>
                                    <RectangleGeometry Rect="0,0,30,1.54" />
                                    <RectangleGeometry Rect="0,3.08,30,1.54" />
                                    <RectangleGeometry Rect="0,6.15,30,1.54" />
                                    <RectangleGeometry Rect="0,9.23,30,1.54" />
                                    <RectangleGeometry Rect="0,12.31,30,1.54" />
                                    <RectangleGeometry Rect="0,15.38,30,1.54" />
                                    <RectangleGeometry Rect="0,18.46,30,1.54" />
                                </GeometryGroup>
                            </GeometryDrawing.Geometry>
                        </GeometryDrawing>
                        <GeometryDrawing Brush="#3C3B6E"><GeometryDrawing.Geometry><RectangleGeometry Rect="0,0,12,10.77" /></GeometryDrawing.Geometry></GeometryDrawing>
                        <GeometryDrawing Brush="#FFFFFF">
                            <GeometryDrawing.Geometry>
                                <GeometryGroup>
                                    <EllipseGeometry Center="2,2.2" RadiusX="0.6" RadiusY="0.6" />
                                    <EllipseGeometry Center="5,2.2" RadiusX="0.6" RadiusY="0.6" />
                                    <EllipseGeometry Center="8,2.2" RadiusX="0.6" RadiusY="0.6" />
                                    <EllipseGeometry Center="11,2.2" RadiusX="0.6" RadiusY="0.6" />
                                    <EllipseGeometry Center="3.5,4.3" RadiusX="0.6" RadiusY="0.6" />
                                    <EllipseGeometry Center="6.5,4.3" RadiusX="0.6" RadiusY="0.6" />
                                    <EllipseGeometry Center="9.5,4.3" RadiusX="0.6" RadiusY="0.6" />
                                    <EllipseGeometry Center="2,6.4" RadiusX="0.6" RadiusY="0.6" />
                                    <EllipseGeometry Center="5,6.4" RadiusX="0.6" RadiusY="0.6" />
                                    <EllipseGeometry Center="8,6.4" RadiusX="0.6" RadiusY="0.6" />
                                    <EllipseGeometry Center="11,6.4" RadiusX="0.6" RadiusY="0.6" />
                                    <EllipseGeometry Center="3.5,8.5" RadiusX="0.6" RadiusY="0.6" />
                                    <EllipseGeometry Center="6.5,8.5" RadiusX="0.6" RadiusY="0.6" />
                                    <EllipseGeometry Center="9.5,8.5" RadiusX="0.6" RadiusY="0.6" />
                                </GeometryGroup>
                            </GeometryDrawing.Geometry>
                        </GeometryDrawing>
                    </DrawingGroup>
                </DrawingImage.Drawing>
            </DrawingImage>

            <!-- ============================ Texto ============================ -->
            <Style x:Key="BodyText" TargetType="TextBlock">
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>

            <Style x:Key="CardTitle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="17" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Foreground" Value="{DynamicResource Primary}" />
            </Style>

            <Style x:Key="HintText" TargetType="TextBlock">
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Foreground" Value="{DynamicResource TextSecondary}" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>

            <!-- ============================ Superficies ============================ -->
            <Style x:Key="Card" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource CardBackground}" />
                <Setter Property="CornerRadius" Value="12" />
                <Setter Property="Padding" Value="12" />
            </Style>

            <!-- ============================ Botones ============================ -->

            <!-- Botones de accion: solo icono, nunca palabras (constitucion 6). -->
            <Style x:Key="IconButton" TargetType="Button">
                <Setter Property="FontFamily" Value="Segoe Fluent Icons, Segoe MDL2 Assets" />
                <Setter Property="FontSize" Value="16" />
                <Setter Property="Width" Value="36" />
                <Setter Property="Height" Value="36" />
                <Setter Property="Margin" Value="2,0" />
                <Setter Property="Foreground" Value="{DynamicResource OnPrimary}" />
                <Setter Property="Background" Value="{DynamicResource Primary}" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Focusable" Value="False" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.85" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.4" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="GhostIconButton" TargetType="Button" BasedOn="{StaticResource IconButton}">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{DynamicResource TextSecondary}" />
            </Style>

            <Style x:Key="DangerIconButton" TargetType="Button" BasedOn="{StaticResource IconButton}">
                <Setter Property="Background" Value="{DynamicResource Danger}" />
            </Style>

            <!-- Boton con texto y borde, para los idiomas y el contacto de «Acerca de». -->
            <Style x:Key="OutlineButton" TargetType="Button">
                <Setter Property="FontFamily" Value="Segoe UI" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Height" Value="36" />
                <Setter Property="Padding" Value="16,0" />
                <Setter Property="Foreground" Value="{DynamicResource Primary}" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                    BorderBrush="{DynamicResource Primary}" BorderThickness="1" CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                  Margin="{TemplateBinding Padding}" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.85" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Boton con estado (siempre encima): relleno de marca cuando esta activo. -->
            <Style x:Key="ToggleIconButton" TargetType="ToggleButton">
                <Setter Property="FontFamily" Value="Segoe Fluent Icons, Segoe MDL2 Assets" />
                <Setter Property="FontSize" Value="16" />
                <Setter Property="Width" Value="36" />
                <Setter Property="Height" Value="36" />
                <Setter Property="Margin" Value="2,0" />
                <Setter Property="Foreground" Value="{DynamicResource TextSecondary}" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Focusable" Value="False" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Primary}" />
                                    <Setter Property="Foreground" Value="{DynamicResource OnPrimary}" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.85" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Selector de modo del chat (Preguntas / Agente): dos segmentos, el activo relleno de marca. -->
            <Style x:Key="SegmentButton" TargetType="ToggleButton" BasedOn="{StaticResource ToggleIconButton}">
                <Setter Property="FontFamily" Value="Segoe UI" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Width" Value="Auto" />
                <Setter Property="Height" Value="30" />
                <Setter Property="Margin" Value="0" />
                <Setter Property="Padding" Value="12,0" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Primary}" />
                                    <Setter Property="Foreground" Value="{DynamicResource OnPrimary}" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.85" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!--
                Desplegable de dispositivos con plantilla propia. La de fabrica de WPF pinta el
                desplegable y la lista en blanco pase lo que pase: en tema oscuro salia una caja
                clara con texto que no se leia. Aqui todo sale de los tokens del tema.
            -->
            <Style x:Key="DeviceComboItem" TargetType="ComboBoxItem">
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Padding" Value="10,6" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBoxItem">
                            <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                <ContentPresenter />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsHighlighted" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Primary}" />
                                    <Setter Property="Foreground" Value="{DynamicResource OnPrimary}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="DeviceCombo" TargetType="ComboBox">
                <Setter Property="MinWidth" Value="220" />
                <Setter Property="Height" Value="36" />
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                <Setter Property="Background" Value="{DynamicResource PageBackground}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Separator}" />
                <Setter Property="ItemContainerStyle" Value="{StaticResource DeviceComboItem}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <ToggleButton x:Name="Toggle" Focusable="False" ClickMode="Press"
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border Background="{Binding Background, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                    BorderBrush="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                    BorderThickness="1" CornerRadius="10">
                                                <TextBlock Text="&#xE70D;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="12"
                                                           Foreground="{DynamicResource TextSecondary}"
                                                           HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" />
                                            </Border>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <ContentPresenter x:Name="Selected" IsHitTestVisible="False"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                                  Margin="12,0,30,0" VerticalAlignment="Center">
                                    <ContentPresenter.Resources>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                                            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                        </Style>
                                    </ContentPresenter.Resources>
                                </ContentPresenter>
                                <!-- Editable (IsEditable): la caja donde se escribe. WPF la busca por este nombre. -->
                                <TextBox x:Name="PART_EditableTextBox" Visibility="Collapsed" Background="Transparent"
                                         BorderThickness="0" Foreground="{DynamicResource TextPrimary}"
                                         CaretBrush="{DynamicResource TextPrimary}" VerticalContentAlignment="Center"
                                         Margin="8,0,30,0" Padding="0"
                                         IsReadOnly="{TemplateBinding IsReadOnly}" Focusable="True" />
                                <!-- Sin StaysOpen="False": el ComboBox captura el raton al abrir y cierra el
                                     desplegable el mismo; con StaysOpen en falso los dos se pelean por la
                                     captura y el desplegable se cerraba en el mismo clic que lo abria. -->
                                <Popup x:Name="PART_Popup" IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom"
                                       AllowsTransparency="True" PopupAnimation="Fade" Focusable="False">
                                    <Border Background="{DynamicResource CardBackground}"
                                            BorderBrush="{DynamicResource Separator}" BorderThickness="1"
                                            CornerRadius="10" Padding="4" Margin="0,4,0,0"
                                            MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                        <ScrollViewer VerticalScrollBarVisibility="Auto" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Selected" Property="Opacity" Value="0.6" />
                                </Trigger>
                                <Trigger Property="IsEditable" Value="True">
                                    <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                                    <Setter TargetName="Selected" Property="Visibility" Value="Hidden" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ============================ Campos ============================ -->
            <Style x:Key="Field" TargetType="TextBox">
                <Setter Property="Height" Value="34" />
                <Setter Property="Padding" Value="8,0" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                <Setter Property="Background" Value="{DynamicResource PageBackground}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Separator}" />
                <Setter Property="CaretBrush" Value="{DynamicResource TextPrimary}" />
            </Style>
            <Style x:Key="PasswordField" TargetType="PasswordBox">
                <Setter Property="Height" Value="34" />
                <Setter Property="Padding" Value="8,0" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                <Setter Property="Background" Value="{DynamicResource PageBackground}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Separator}" />
                <Setter Property="CaretBrush" Value="{DynamicResource TextPrimary}" />
            </Style>
            <Style x:Key="FieldLabel" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource TextSecondary}" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Margin" Value="0,10,0,4" />
            </Style>
            <Style x:Key="Check" TargetType="CheckBox">
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                <Setter Property="Margin" Value="0,8,0,0" />
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
